# Repo 導覽

> 71 個檔案、約 11,000 行。這份文件是地圖。
> 想快速了解這個專案在做什麼，照章節順序讀。

---

## 一分鐘版本

這個專案有兩條主線：

**一、拉密遊戲引擎與對戰 AI**（第 1–2 章）
規則、盤面、四支不同性格的 AI。對 baseline 千場測試 82% 勝率。

**二、認知教練系統**（第 3–5 章）
不是「幫你出牌」，是「教會你自己出」。
核心問題只有一個：**什麼時候該說話、說到多深、什麼時候該閉嘴。**

加上一章驗證（第 6 章）—— 上面所有設計憑什麼。

---

## 兩個實作，一條演進線

這件事要先講清楚，否則會不知道該改哪邊。

```
src/     ── 能玩的那條路 ──────────────────────
         game_manager → cognitive_coach_agent
                      → coach_hint_bridge → state.json
         六關卡在 coach_campaign
         提示邏輯綁在拉密上（知道什麼是 Run、Joker）

coach/   ── 抽象重寫版 ────────────────────────
         coach_engine 完全不知道拉密的存在
         領域只要能回答三個問題就能接上
         兩個領域驗證：rummikub_domain、gridnav_domain
         測試全過，但**還沒接進遊戲主流程**
```

**沒有任何 `src/` 檔案 include `coach/`。** 兩邊目前平行存在。

`coach/` 是想要的樣子，`src/` 是現在能跑的樣子。
遷移還沒做 —— 這是已知的技術債，不是疏漏。

---

# 第 1 章 · 遊戲引擎

**回答：這一手合不合法？**

| 檔案 | 負責 |
|---|---|
| `src/tile.h` | 牌：數字、顏色、Joker |
| `src/board.h` | 桌面上的組合 |
| `src/player.h` | 手牌與分數 |
| `src/validator.h` | **規則判定** —— Run、Group、Joker 代位 |
| `src/game_manager.h` | 回合流程、狀態匯出 |

`validator` 是整個專案的地基。它有 266 行測試守著，
因為所有其他模組都預設「規則判定是對的」。

**先讀這章**，後面每一章都建立在這裡。

---

# 第 2 章 · 對戰 AI

**回答：該出哪張牌？**

| 檔案 | 性格 |
|---|---|
| `src/ai_agent_baseline0.h` | 最陽春，當下限對照組 |
| `src/ai_agent_baseline1.h` | 稍強的對照組 |
| `src/ai_agent_0.h` | **主力**，含「大風吹」全域重組 |
| `src/ai_agent_conservative.h` | 保守：留牌、少動桌面 |
| `src/ai_agent_aggressive.h` | 積極：能出就出 |

有兩個對照組是刻意的 —— 沒有下限，「82% 勝率」說不出是好是壞。

`ai_agent_0` 的「大風吹」是把桌面拆開重拼，讓手上的牌接得上。
設計文件：`docs/strategies/02_大風吹_windstorm.md`

---

# 第 3 章 · 教練引擎 ⭐

**回答：什麼時候該說話、說到多深？**

這是整個專案的核心，也是唯一**不綁死在拉密上**的部分。

### 界線

```
領域知道「答案是什麼」，引擎決定「要不要說、說多少」。
```

領域只要能回答三個問題，就能接上這個引擎：

| 方法 | 做什麼 |
|---|---|
| `solve()` | 給我現在的狀態，找出一個解 |
| `hint()` | 把那個解翻譯成三種深淺的說法 |
| `classify()` | 從動作前後的狀態，反推用了哪些技巧 |

### 檔案

| 檔案 | 內容 |
|---|---|
| `coach/coach_engine.h` | 抽象引擎。決定提示層級、記錄掌握度、保底機制 |
| `coach/domains/rummikub_domain.h` | 拉密領域，六招全部接進來 |
| `coach/domains/gridnav_domain.h` | **第二個領域**：機器人格點導航 |
| `coach/coach_modes.h` | 五種音量 |
| `src/cognitive_hint_engine.h` | 舊版，綁在拉密上，但這是遊戲實際在用的 |
| `src/coach_campaign.h` | 六關卡（舊版路徑） |
| `src/technique_detector.h` | 從盤面反推用了哪一招 |

### gridnav_domain 是驗證，不是功能

它跟拉密沒有任何共同點 —— 狀態是「機器人在哪」，動作是「往哪走一步」，
技巧是「繞障礙」「走對角」。

**但引擎一行都沒改。** 抽象層的驗證條件就是這一句。

### 六個關卡

| Level | 技巧 | 引導強度 | 提示上限 |
|---|---|---|---|
| 1 | 接龍頭尾 | 100% | 講答案 |
| 2 | 補第四色 | 88% | 講答案 |
| 3 | Joker 補缺口 | 76% | 講答案 |
| 4 | 破冰湊 30 分 | 64% | 指方向 |
| 5 | 大風吹重組 | 52% | 指方向 |
| 6 | 長龍切斷 | 40% | 輕推 |

「引導強度」不是抽象的百分比，它落實成兩件事：
**卡幾回合才開口**、**最多給到哪一層**。

### 掌握度只升不降

| | 條件 |
|---|---|
| ⭐ COPIED | 看了答案才做出來 |
| ⭐⭐ PROMPTED | 只給方向就自己找到 |
| ⭐⭐⭐ DISCOVERED | 完全沒提示 |

過關要「用出 3 次，其中 N 次自主」——
前者確保熟練，後者確保是真的懂了而不是照抄。

### 五種模式

同一個引擎、同一套關卡，差別只在門檻乘上一個係數。

新手練組 → 炫酷組 → 較量組 → 挑戰極限組 → 高手過招（Coach 退場）

> 一個「什麼時候該說話」的系統，如果只有一種音量，
> 那它其實沒有回答那個問題，只是選了一個答案。

---

# 第 4 章 · 教學模組

**回答：學會了沒？什麼時候該複習？**

| 檔案 | 內容 |
|---|---|
| `coach/recap/recap.h` | 每關結束 5 題 MCQ，共 30 題 |
| `coach/spaced_cycle.h` | 間隔重複，費氏數列循環 |
| `coach/audience_profile.h` | 兒童版 vs 長者版 |
| `coach/companions.h` | 三個角色：引路、見證、回望 |
| `coach/battle/mini_battle.h` | 玩家自訂挑戰規則 |
| `coach/battle/battle_parser.h` | 挑戰腳本的解析器 |
| `coach/coach_session.h` | 把上面接成一個系統 |

### Recap 的第 5 題最關鍵

五題的題型固定：辨識 → 判斷合法 → 選最佳 → 理解機制 → **換情境問同一招**。

前四題測「記不記得剛才那步」，第五題測「換場景還會不會用」。

### 間隔重複是循環，不是封頂

```
1 → 2 → 3 → 5 → 8 → 13 → 1 → 2 → 3 → 5 → ...
```

封頂在 13 的話，目標是「記得住」。
循環回 1 的話，目標是「持續使用」。

**對延緩失智這個定位，循環才對。**

### 兩個族群，方法論相反

| | 兒童版 | 長者版 |
|---|---|---|
| 目標 | 習得新能力 | 維持既有功能 |
| 犯錯 | 有價值 | **有風險**（學會錯誤） |
| 引導 | 100% → 40% 遞減 | 依表現動態調整 |
| 卡關 16 回合 | 挑戰 | 挫折，可能放棄 |

長者版採無錯學習（errorless learning），
依據是 Clare & Jones 2008、de Werd et al. 2013。

### 「回望」是唯一由玩家觸發的角色

它給的不是這一手的答案，是「你以前怎麼做的」——
只講規則，不指牌。

---

# 第 5 章 · 強化學習

**回答：能不能不靠規則，自己學出策略？**

| 檔案 | 內容 |
|---|---|
| `rl/actor_critic.h` | 手刻的 Actor-Critic，沒有用框架 |
| `rl/mini_env.h` | 簡化版拉密：桌面一條 Run、手上 5 張、6 種動作 |
| `rl/test_rl.cpp` | 含梯度檢查 |

### 為什麼手刻

網路只有幾百個參數。手刻的話每一步梯度都看得見 ——
呼叫 `loss.backward()` 學不到那些東西。

`test_rl.cpp` 有梯度檢查：解析梯度與數值微分吻合到 `1e-11`。

### 為什麼縮到最小

完整拉密的動作空間有數萬種、狀態變長、獎勵稀疏，
任何一件都足以讓 Actor-Critic 訓不起來。

所以先做一個**人一眼看得出正確答案**的版本 ——
正因為如此，才能驗證 AI 是不是真的學會了。

---

# 第 6 章 · 驗證

**回答：上面所有設計憑什麼？**

這一章是這個專案跟一般作品最不一樣的地方。

### 測試

```bash
./run_tests.sh          # 全部，484 項
./run_tests.sh coach    # 只跑名稱含 coach 的
./run_tests.sh -k       # 第一個失敗就停
```

| 分組 | 內容 |
|---|---|
| 遊戲引擎 | validator、Board/Player/GM、關卡與偵測器 |
| 教練引擎 | 抽象引擎（假領域）、拉密領域、五種模式、整合層 |
| 教學模組 | 自訂挑戰、Recap MCQ |
| 強化學習 | Actor-Critic |

**「抽象引擎（假領域）」這一組最關鍵** ——
它用一個假領域測引擎，證明引擎確實不依賴拉密。

### 模擬實驗

| 位置 | 驗證什麼 |
|---|---|
| `experiments/learner_simulation.cpp` | **時序** —— 門檻會不會太早或太晚 |
| `experiments/coach_simulation/` | **內容** —— 三層提示各自傳遞了什麼 |

兩者的差別在「學習者的能力」怎麼表示：
前者是機率參數，後者是**已知概念的集合**。

### 一個實際改掉設計的發現

模擬顯示 L6 完全新手平均連續卡 **12 回合**、保底觸發 **8.3%**。

診斷：L6 上限只到「輕推」，而輕推原本說的是
「桌上有一條龍長到可以切開」—— 那是**狀態**，不是**規則**。

對一個不知道「Run 可以切」的人，這句話等於什麼都沒說。

**改動**：六句輕推全部從狀態描述改成規則說明。
現在 L6 的輕推說的是「六張以上的順子切成兩段，就多出兩個可以接牌的位置」。

同樣不透露是哪張牌、接到哪裡，但學習者的搜尋空間真的擴大了。

---

## 已知限制與技術債

**`coach/` 還沒接進遊戲。** 五個模組加兩個領域都有測試，但主流程仍走 `src/`。

**`coach_hint_bridge` 用全域變數當信箱。** 檔案註解已說明這是刻意的最小侵入取捨，
接更多前端資訊時值得改成 Observer。

**提示文字沒有測試守著。** 改壞那六句話，484 項測試不會叫。

**技巧偵測寧可漏判不可誤判。** 「大風吹」與「長龍切斷」無法百分之百區分
「刻意重組」與「剛好排列順序不同」，採最嚴格判準。

**Joker 只填內部缺口，不往兩端延伸。** 刻意保留的限制。

**模擬不是使用者測試。** 學習者的能力參數與難度衰減係數都是假設，
只能回答「門檻的時序是否合理」，不能回答「真人是否會因此學得更好」。

---

## 從哪裡開始讀

| 你想知道 | 讀哪裡 |
|---|---|
| 這遊戲怎麼運作 | 第 1 章 → `src/validator.h` |
| AI 怎麼決定出牌 | 第 2 章 → `docs/strategies/` |
| **這個專案最特別的地方** | 第 3 章 → `coach/coach_engine.h` |
| 抽象層是真的嗎 | `coach/domains/gridnav_domain.h` |
| 這些設計憑什麼 | 第 6 章 → `experiments/` |
